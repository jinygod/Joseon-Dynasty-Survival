# 전투 마스터리 수직 슬라이스 보고서

이 문서는 스키마 1·2 원본 텔레메트리의 자동 집계와 실제 플레이 수동 검증을
분리한다. 원본 CSV/JSON은 보존하며, 집계값만으로 시각·조작 경험을 판정하지 않는다.

## 자동화 증거

`TelemetryExportService.aggregate`는 다음 형식화된 집계값을 제공한다.

- 무기별 제안·선택 횟수와 선택률
- 무기별 피해 비중, 레벨 도달 평균 시각, 최초 마스터 평균 시각과 마스터 후
  10초 평균 처치 수
- 시너지별 최초 발동 평균 시각, 피해 비중과 전체 시너지 피해 비중
- 적 역할별 플레이어 피해량과 사망 원인 횟수
- 평균·최대 적 밀도, 후반 평균·최저 FPS
- 마스터 런과 비마스터 런의 승률, 반복 런 비율

0인 분모는 0으로 보고하며 스키마 1의 비어 있는 전투 지표는 누락 데이터로
안전하게 처리한다. 후반 FPS의 0 값은 후반 표본이 없는 조기 종료 런을 뜻하므로
평균의 분모와 최저값에서 제외하며, 관측 런이 하나도 없으면 두 값 모두 0으로
보고한다. 원본 내보내기 경로인 단일 런 JSON 복사와 전체 JSON 공유는 변경하지
않는다.

자동 검증 명령:

```powershell
flutter test test/game/telemetry_export_service_test.dart test/game/run_telemetry_test.dart
```

## 2026-07-22 최종 완료 게이트 증거

승인 증거는 시각 통합 성능 기준선 갱신 커밋
`783c3f06a88eaf5e5160604888d5c03e765d2e7a` 하나에서 시작하고 끝난 실행만 사용한다.
각 주요 게이트 직전과 직후에 같은 HEAD와 깨끗한 작업 트리를 확인했다. 외부 배포,
merge, push는 수행하지 않았다.

Flutter SDK는 `C:\Users\전성진\source\flutter`를 가리키는 ASCII `Y:` 드라이브를
사용했다. `Z:\` 드라이브 루트의 Dart 3.12 분석 서버 세션 로그 오류를 피하기 위해
분석, 전체 테스트, web 빌드와 APK 빌드는
`D:\codex-final-783c3f0\repo`의 detached 물리 체크아웃에서 실행했다. 이 복사본은
Git 추적 파일 638개의 SHA-256이 원본과 전부 일치했고 불일치는 0개였다.

| 게이트 | 실행 시각 (KST) | 종료 코드 | 관측 결과 |
| --- | --- | ---: | --- |
| `git diff --check` 및 `dart format --output=none --set-exit-if-changed lib test` | 04:02:55.359–04:02:56.903 | 0 | 274개 파일 검사, 변경 0, 공백 오류 없음 |
| `flutter analyze --no-pub` | 최종 HEAD 재검증 | 0 | `No issues found!` (분석 17.2초) |
| `flutter test --no-pub -r compact` | 최종 HEAD 재검증 | 0 | 정확히 748개 통과, `All tests passed!` (128초) |
| `flutter build web --release --no-pub` | 최종 HEAD 재검증 | 0 | 73개 파일, 합계 43,762,246 bytes (59.7초) |
| 고정 시드 5분 슬라이스 | 전체 테스트에 포함 | 0 | 300초/18,000 프레임 회귀 기준 통과 |
| `flutter build apk --debug --no-pub` | 최종 HEAD 재검증 | 0 | Gradle 348.3초, 아래 APK 생성 |

web 산출물은
`D:\codex-final-783c3f0\repo\build\web`에 있으며 73개 파일,
43,762,246 bytes이다. APK는 같은 깨끗한 물리 체크아웃에서 새로 만들고 표준 경로에
복사한 뒤 SHA-256, 크기와 수정 시각이 모두 같은지 확인했다.

- 임시 빌드 경로: `D:\codex-final-783c3f0\repo\build\app\outputs\flutter-apk\app-debug.apk`
- 표준 경로: `build/app/outputs/flutter-apk/app-debug.apk`
- 크기: 162,761,059 bytes
- SHA-256: `EC9237A036F595CAC2564004EEBFEDD453083E0C26B21F0CA92F79A4F55F1837`
- 수정 시각: 2026-07-22 05:15:51.745 KST

승인 순서 전에 C: 디스크 부족으로 끝난 테스트, 이전 성능 기준선이 남아 744개 통과와
1개 실패를 기록한 테스트, 외부 테스트 프로세스가 공유 `build/unit_test_assets`를
점유해 `shaders/ink_sparkle.frag`가 누락된 테스트는 환경 또는 기준선 수정 전 실행이다.
이 실행들의 로그와 산출물은 모두 최종 승인 증거에서 제외했다.

### 고정 시드 밀도·성능 및 컴포넌트 값

시드 `3107`, 300초, 18,000 논리 프레임의 새 `build/qa` 산출물에서 확인한 값이다.

- 전체 평균/최대 활성 적: 15.68288888888889 / 51
- 후반 평균/최대 활성 적: 27.236944444444443 / 51 (7,200 표본)
- 후반 평균/최저 시뮬레이션 FPS: 59.998800023996225 / 59.99880002399952
- 적/투사체/피해 숫자/전투 효과 최대치: 51 / 14 / 24 / 23
- 인구 제한 위반 표본: 0, 메모리 프록시 위반 표본: 0
- 최대 mounted component: 262, 최대 유지 소유자: 13/128,
  최대 메모리 프록시: 272/512
- 모든 인구·컴포넌트·소유자·메모리 프록시 예산 판정: 통과
- host test-loop의 `update`+lifecycle 최대 관측 시간: 6,318 microseconds
- 고정 시드 런의 보스 요청과 실제 보스 생성: 각각 1회
- 시드 3107의 240–270초 혼합 역할 생성: 쥐떼 33, 원혼 20, 삿갓 망령 21,
  도깨비 23, 장승령 7, 처녀귀 6으로 모두 0보다 큼
- 시드 3107 회귀: 보스 요청 1, 총 생성 641, 최대 활성 적 96, 프레임당 최대 생성 3,
  잘못된 풀 요청 0, cap 위반 0
- 20개 시드 회귀는 시드별 보스 요청 1회, 최대 활성 적 96 이하, 프레임당 생성 8
  이하, 총 생성 400 이상, pressure 구간 elite 합계가 0보다 큼을 검증

원본 자동 산출물은 격리 복사본의
`build/qa/production-high-risk-performance-log.json`과
`build/qa/production-high-risk-performance-log.md`에 있다. 위 FPS는 고정된 원시
프레임 간격에서 계산한 결정론적 호스트 시뮬레이션 값이다. host wall time도 개발
호스트 진단값일 뿐 실제 모바일 기기 FPS 또는 프레임 시간 측정값이 아니다.

## 수동 검증 — 실제 플레이 결과 미기록

아래 항목은 자동 테스트 결과가 아니다. 관찰자가 실제 빌드를 플레이하고 근거를
기록하기 전까지 모든 결과 칸을 비워 둔다.

| 번호 | 승인된 실제 플레이 질문 | 관찰 결과 |
| --- | --- | --- |
| 1 | 플레이어, 일반 적, 엘리트와 보스가 모바일 화면에서 실루엣으로 구분되는가? | - [ ] |
| 2 | 환도 레벨 1, 3, 4, 5와 6이 서로 다른 공격으로 보이는가? | - [ ] |
| 3 | 부적의 부착, 폭발, 전이, 소형 결계와 마스터 결계가 읽히는가? | - [ ] |
| 4 | 마스터 발동 순간 무기가 잠시 화면의 주인공처럼 보이는가? | - [ ] |
| 5 | 봉마참 이름, 색상과 소리가 일반 공격과 구분되는가? | - [ ] |
| 6 | 적 공격 경고가 마스터·시너지 효과 위에서도 보이는가? | - [ ] |
| 7 | 원거리형 때문에 접근 경로를 바꾸고 돌진형 때문에 회피하게 되는가? | - [ ] |
| 8 | 방어형의 정면과 측후면 대응 차이를 시각적으로 이해할 수 있는가? | - [ ] |
| 9 | 240~270초에 일반 적을 대량 처치하면서 엘리트와 보스는 남는가? | - [ ] |
| 10 | 마스터가 없어도 회피와 운영으로 생존할 수 있는가? | - [ ] |
| 11 | 화면 흔들림과 히트스톱이 과하지 않고 끈 설정이 존중되는가? | - [ ] |
| 12 | 두 번째 런을 실제로 다시 시작하고 싶었는가? | - [ ] |

수동 관찰 근거: 아직 기록하지 않음.

실제 모바일 기기의 프로필 모드 RSS·heap·raster/frame timing, 실제 화면에서의 공격
방향·타격 정렬·마스터 크기·적 경고 가시성·프레임 안정성, 그리고 주관적인 재미와
두 번째 런 의향은 모두 미검증 상태로 남는다.
